[tool:pytest]
markers =
    performance: Performance tests (slow, may require running server)
    slow: Slow tests that take a long time to run
    integration: Integration tests that require external services
